header.header
    nav.nav.nav--tours
        a.nav__el(href='/') CP Natation
    a.header__logo(href='/')
        svg(style="height: 50px; width: 50px;").card-me__icon
            use(xlink:href=`img/sprite.svg#icon-graphcool`)
    nav.nav.nav--user
        if locals.user
            a.nav__el(href='/dashboard') #{locals.user.firstName}
            a.nav__el.nav__el--logout Log out
            
        else
            a.nav__el(href='/login') Log in
            a.nav__el.nav__el--cta(href='#') Sign up
